language: d

d:
  - dmd-2.078.2
  - dmd-2.073.2
  - dmd-2.072.2

services:
  - mongodb

addons:
  apt:
    packages:
      - pkg-config
      - zlib1g-dev
      - libevent-dev
      - libssl-dev

sudo: false

script:
  - dub upgrade --missing-only # attempt to work around DUB build issue
  - dub test
